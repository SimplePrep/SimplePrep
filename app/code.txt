
docker-compose run --rm app sh -c "python manage.py "

sudo yum update -y

sudo amazon-linux-extras install -y docker

sudo systemctl enable docker.service

sudo systemctl start docker.service

sudo usermod -aG docker ec2-user

wget https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)

sudo mv docker-compose-Linux-x86_64 /usr/local/bin/docker-compose

sudo chmod +x /usr/local/bin/docker-compose

sudo yum install -y git

ssh-keygen -t ed25519 -C "GitHub Deploy Key"

 cat ~/.ssh/id_ed25519.pub

git clone git@github.com:Artint-AI-C/SimplePrep.git

docker-compose -f docker-compose-deploy.yml run --rm certbot /opt/certify-init.sh

sudo ./init-letsencrypt.sh

docker-compose -f docker-compose-deploy.yml down #just a comment